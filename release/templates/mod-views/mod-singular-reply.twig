<!DOCTYPE html>
<HTML>
  <HEAD>
    {% include 'mod-views/mod-header.twig' %}
    <TITLE>Kissu Feedback & Forms - {{ form.ID }}'s Reply from {{ reply.Identifier }}'</TITLE>
  </HEAD>
  <BODY>
  <H1>{{ reply.Identifier }}'s response to: {{ form.FormName }}</H1>
    <P>Description: {{ form.Description }}</P>
    <A href="/mod/view/{{ reply.FK_ID }}">Back to Form</A>
    <OL>
      <LI id="row-{{ form.FormName }}-{{ reply.ID }}">
        <div>
          Responder: {{ reply.Identifier }} <br/>
          SubmittedAt: {{ reply.SubmittedAt }}<br/>
          <A href="/mod/response/delete/{{ form.FormName }}/{{ reply.ID }}" onclick="return FormLibrary.postDeleteResponse('{{ form.FormName }}' ,'{{ reply.ID }}')">DELETE</A>
        </div>
        {% for field in form.FormFields %}
          {% include 'mod-views/mod-reply-item.twig' with {'field':field , 'reply': reply} %}
        {% endfor %}
        <br/>
      </LI>
    </OL>
    <HR/>
    {% include 'mod-views/mod-footer.twig' %}
  </BODY>
</HTML>
